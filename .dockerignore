# Dependencies and build output are recreated inside Docker build stages.
node_modules
.next

# Source-control and local configuration must not be copied into the image.
.git
.gitignore
.env
.env.*
!.env.example

# Local editor, log and coverage files.
*.log
coverage
.DS_Store
