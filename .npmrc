# Prevent pnpm from verifying/reinstalling dependencies when running scripts (e.g. pnpm start in production containers)
verify-deps-before-run=never
enable-global-virtual-store=false
update-notifier=false
